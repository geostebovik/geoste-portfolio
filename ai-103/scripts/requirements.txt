requests
python-dotenv
openai
# Pinned 2026-08-05 — verified _rename_columns_conditionally() behavior against this
# exact version (venv1\Lib\site-packages\azure\ai\evaluation\_evaluate\_evaluate.py:797).
# Bump deliberately, re-verify if evaluate()'s behavior around unmapped columns matters again.
azure-ai-evaluation==1.18.3
