# The Function's own dependencies (M11). NOT scripts/requirements.txt, which is
# the whole lab's.
#
# FULLY PINNED, 2026-09-24 (Claude, from Gerard's `pip freeze` of the local venv,
# Python 3.14.0). Every package the measured code ran on is pinned, not just the
# direct ones, so a remote build installs exactly the set M10 measured instead
# of whatever is newest that day.
#   dropped from the freeze: playwright, pyee, greenlet (playwright only),
#     azure-search-documents (M5 only), colorama (a Windows-only dependency)
#   kept: python-dotenv, which six chain modules import at module scope
#   added, NOT in the venv so NOT measured: azure-functions (the trigger), and
#     werkzeug, which azure-functions requires
# Checked 2026-09-24 on Linux/Python 3.14.7: installs from wheels only (no
# source builds) in 23.5 s, 300 MB, pulls in nothing unpinned, and the built
# package passes the offline checks in README.md.
#
# To change a version: change it in the venv first, re-run what depends on it,
# then update this line.

# --- direct: the Function ---
azure-functions==2.3.0
werkzeug==3.1.8
azure-storage-blob==12.30.0

# --- direct: the M7 import chain ---
azure-ai-agents==1.1.0
azure-ai-evaluation==1.18.3
azure-ai-vision-imageanalysis==1.0.0
azure-identity==1.25.3
openai==2.53.0
pydantic==2.13.4
numpy==2.5.1
pillow==12.3.0
requests==2.34.2
python-dotenv==1.2.2

# --- transitive, pinned to the measured venv ---
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
azure-core==1.41.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
cryptography==50.0.0
defusedxml==0.7.1
distro==1.9.0
frozenlist==1.8.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
isodate==0.7.2
Jinja2==3.1.6
jiter==0.16.0
joblib==1.5.3
MarkupSafe==3.0.3
msal==1.37.0
msal-extensions==1.3.1
msrest==0.7.1
multidict==6.7.1
nltk==3.10.2
oauthlib==3.3.1
pandas==2.3.3
propcache==0.5.2
pycparser==3.0
pydantic_core==2.46.4
PyJWT==2.13.0
python-dateutil==2.9.0.post0
pytz==2026.3.post1
regex==2026.7.19
requests-oauthlib==2.0.0
ruamel.yaml==0.19.1
six==1.17.0
sniffio==1.3.1
tqdm==4.70.0
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
yarl==1.24.5
